# Generated by Okty.io
version: '3'
services:
    nginx:
        build: docker/nginx/
        ports:
            - '8083:80'
            - '443:443'
        volumes:
            - './:/usr/share/nginx/html:cached'
    adminer:
        image: 'adminer:latest'
        ports:
            - '8081:8080'
    php:
        build: docker/php/
        volumes:
            - './:/usr/share/nginx/html'
    composer:
        image: 'composer:latest'
        volumes:
            - './:/app'
    db:
        image: 'postgres:11'
        ports:
            - '5433:5432'
        volumes:
            - './docker/database:/var/lib/postgresql/data:z'
        environment:
            - POSTGRES_DB=docker
            - POSTGRES_USER=hackathon
            - POSTGRES_PASSWORD=hackathon19
